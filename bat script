1、延迟N秒关闭窗口的方法

    timeout N  //倒计时N秒
    
    或者 ping 127.0.0.0 -n 1 -w N*1000  //-n 1 attempt to connect once -w N*1000 waits N sec
    
2、命令后加 >null 不显示信息在屏幕

3、start命令启动程序时：

  如果你所在程序的路径中带有空格，那么必须用""把路径括起来。在运行某些程序时，需在路径的前面加一对空白的""，表示创建一个空白的窗口，它指向的程序是XXXXXXXX。
